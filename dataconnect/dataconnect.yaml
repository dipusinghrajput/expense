specVersion: "v1"
serviceId: "newfolder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newfolder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
